{% extends 'base.html.twig' %}

{% block title %}Rejoindre {{ school.name }}{% endblock %}

{% block body %}
<div class="max-w-2xl mx-auto py-20 px-4">
    <div class="text-center space-y-8 animate-in fade-in slide-in-from-bottom-8 duration-700">
        <div class="relative inline-block">
            <div class="absolute inset-0 bg-primary/20 blur-3xl rounded-full scale-150 animate-pulse"></div>
            <div class="relative w-24 h-24 bg-primary rounded-3xl flex items-center justify-center mx-auto shadow-neon-intense">
                <i data-lucide="building" class="w-12 h-12 text-white"></i>
            </div>
        </div>
        
        <div class="space-y-4">
            <h1 class="text-5xl font-extrabold text-white tracking-tight">Rejoindre {{ school.name }}</h1>
            <p class="text-slate-400 text-lg max-w-lg mx-auto leading-relaxed">
                Vous êtes sur le point d'envoyer une demande d'adhésion à cet établissement. Une fois acceptée, vous aurez accès à l'espace d'apprentissage.
            </p>
        </div>

        <div class="bg-surface-dark/40 border border-white/5 rounded-2xl p-8 backdrop-blur-md">
            <div class="flex items-center space-x-4 mb-6 text-left">
                <div class="w-12 h-12 bg-white/5 rounded-full flex items-center justify-center text-slate-500">
                    <i data-lucide="user" class="w-6 h-6"></i>
                </div>
                <div>
                    <p class="text-[10px] font-bold text-slate-500 uppercase tracking-widest">En tant que</p>
                    <p class="text-white font-bold">{{ app.user.firstName }} {{ app.user.name }}</p>
                </div>
            </div>

            <div class="flex flex-col sm:flex-row gap-4">
                <form method="post" class="flex-1">
                    <button type="submit" class="w-full py-4 bg-primary text-white rounded-xl font-bold text-lg hover:brightness-110 transition-all shadow-lg shadow-primary/30 flex items-center justify-center space-x-2">
                        <i data-lucide="check" class="w-6 h-6"></i>
                        <span>Confirmer l'Adhésion</span>
                    </button>
                </form>
                <a href="{{ path('home') }}" class="flex-1 py-4 bg-white/5 text-slate-400 rounded-xl font-bold text-lg hover:text-white hover:bg-white/10 transition-all flex items-center justify-center border border-white/10">
                    Annuler
                </a>
            </div>
        </div>

        <p class="text-xs text-slate-600">
            En rejoignant cet établissement, vous acceptez de partager vos informations de profil avec ses administrateurs.
        </p>
    </div>
</div>
{% endblock %}
